# AI-Native Agent · Phase 1.5 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 Phase 1 的「能跑」补成「可用」——补 6 个真实体验缺口：装齐数据源依赖、工具失败时不推半成品决策卡（改发 `error` 事件）、并发跑 graph 与 LLM 消除 5s 首字延迟（**UX 妥协：traces 仍是在 LLM 流完后一次性 emit，不是实时**）、emit `tool_trace` 事件、threads+messages+decisions 全链路 SQLite 持久化。

**Architecture:** 4 个独立改进（Task 1-4，互不依赖）+ 3 个递进搭积木（Task 5→6→7，会话持久化全链路）。Task 5-7 完成后用户刷新 `/agent` 仍能看到历史会话与消息。

**Tech Stack:** Python 3.11+ / FastAPI / aiosqlite / pytest-asyncio · React 19 / zustand / TypeScript strict。

---

## 前置准备

```bash
cd /vol2/1000/code/stockclaw
# 当前 phase-1/ai-native-agent 分支基础上继续
git checkout phase-1/ai-native-agent
git pull
```

**全局硬约束（沿用 Phase 1）：**
1. **数据层不污染**：`astock.py / gstock.py / market.py / newsradar.py` 永远只返回客观数据
2. **同步数据层卸载**：所有调 `astock.*` 的 `@tool` 必须用 `asyncio.to_thread` 包装
3. **结构化事件优先于 Markdown**：决策卡走独立 `decision_artifact` NDJSON 事件
4. **Rate Limiter 横跨业务**：锁 acquire/release 不用 `async with self._lock`
5. **不引入 CopilotKit**

---

## 文件结构总览

```
backend/
├── pyproject.toml                  # Task 1：加 mootdx/akshare/pandas
├── agents/
│   ├── state.py                    # Task 3：加 tool_traces 字段
│   ├── nodes/decision.py           # Task 2：current_price=0 拒推卡；Task 3：收 tool_traces
│   └── runner.py  ← 实际是 backend/runner.py
├── runner.py                       # Task 3：tool_trace 事件；Task 4：并发 graph+LLM；Task 7：写 decisions 表
├── app.py                          # Task 5：加 5 个 /api/agent/threads* 端点
└── tests/
    ├── test_agents_decision.py     # Task 2/3：扩测试
    ├── test_runner.py              # Task 3/4/7：扩测试
    └── test_agent_threads_api.py   # Task 5：新建

frontend/src/
├── lib/api.ts                      # Task 6：校对 agentApi 签名
├── lib/stores/agent.ts             # Task 6：加 loadThreads / fetchThreads action
├── components/agent/AgentSidebar.tsx    # Task 6：onMount fetch + 删除/重命名按钮
└── hooks/useAgentStream.ts         # Task 6：无 threadId 时先 POST 创建；流式结束后归档消息
```

---

## Task 1：装齐数据源依赖（mootdx + akshare + pandas）

**目标：** Phase 1 venv 缺 mootdx/akshare，导致 `astock.kline()` 抛异常 → A 股也走 `model_fallback`，basis_type 色标失去区分意义。装齐后 A 股恢复完整 ATR 公式（model）。

**Files:**
- Modify: `backend/pyproject.toml`

- [ ] **Step 1.1：检查 venv 现状**

```bash
cd /vol2/1000/code/stockclaw/backend
.venv/bin/python -c "import mootdx" 2>&1 | head -3
```

期望：`ModuleNotFoundError: No module named 'mootdx'`。

- [ ] **Step 1.2：往 pyproject.toml 加依赖**

打开 `backend/pyproject.toml`，找到 `[project].dependencies` 块。现在的状态（来自 Phase 1 Task 9 follow-up）：

```toml
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "requests",
    "akshare>=1.10",
    "mootdx>=0.10",
    "pandas",
    "langgraph>=1.0",
    "langchain-core>=1.0",
    "aiosqlite>=0.20",
    "httpx>=0.25,<0.26",
]
```

如果 `akshare`/`mootdx`/`pandas` 已经在列表里，跳到 Step 1.3。否则补上：

```toml
    "akshare>=1.10",
    "mootdx>=0.10",
    "pandas",
```

注：`mootdx>=0.10` 与 `httpx<0.26` 的约束关系——mootdx 0.11.x 要求 `httpx>=0.25,<0.26`，我们已经 pin 在这个区间，OK。

- [ ] **Step 1.3：用 uv sync 装**

```bash
cd /vol2/1000/code/stockclaw
uv sync --active
```

期望：装上 mootdx、akshare、pandas 及其传递依赖（~50 个包，~3 分钟）。

- [ ] **Step 1.4：验证导入**

```bash
cd backend
.venv/bin/python -c "
import mootdx, akshare, pandas
print('mootdx ok')
print('akshare ok')
print('pandas', pandas.__version__)
"
```

期望：三个模块都能 import，无报错。

- [ ] **Step 1.5：跑回归确保没破其他测试**

```bash
.venv/bin/python -m pytest -m "not live" --ignore=tests/test_api.py -v 2>&1 | tail -5
```

期望：60 passed（Phase 1 基线保持）。

- [ ] **Step 1.6：commit**

```bash
cd /vol2/1000/code/stockclaw
git add backend/pyproject.toml
git commit -m "chore(deps): 装 mootdx/akshare/pandas 到 venv

之前 Phase 1 venv 缺这些数据源依赖，导致 A 股 kline 调用抛异常，
所有 A 股决策也走 model_fallback（固定 -8% 止损），basis_type 色标
失去区分意义。装齐后 A 股恢复完整 ATR 公式（model）。"
```

---

## Task 2：current_price=0 时拒推决策卡

**目标：** 当所有工具失败（astock 网络挂、code 找不到、tencent_quote 返回空）时，`current_price=0`，目前仍会推一张全 0 决策卡（目标价 ¥0、止损 ¥0）。修法：decision_node 在 current_price=0 时返回 `{"decision_card": None, "intent": "decision_failed"}`，runner 不发 `decision_artifact`，改发一条 `error` 事件提示用户。

**Files:**
- Modify: `backend/agents/nodes/decision.py`（约第 190-230 行的 `decision_node` 函数）
- Modify: `backend/tests/test_agents_decision.py`（加 1 个测试）

- [ ] **Step 2.1：写失败的测试**

在 `backend/tests/test_agents_decision.py` 末尾追加：

```python
@pytest.mark.asyncio
async def test_decision_node_returns_none_when_all_tools_fail(monkeypatch):
    """所有工具失败（current_price=0）→ 不推半成品决策卡。"""
    from agents.nodes.decision import decision_node

    async def fail_invoke(tool, **kwargs):
        return {
            "tool": tool.name, "error": "mock failure",
            "basis_type": "model_fallback",
            "model_version": f"{tool.name}.v1",
            "outputs": {"fallback_reason": "tool_error"},
            "citations": [], "model_assumptions": [],
        }

    # mock 所有工具失败
    from agents.tools import forward_pe_target, atr_stop, pe_percentile_revert, risk_based_position, batch_build
    for t in [forward_pe_target, atr_stop, pe_percentile_revert, risk_based_position, batch_build]:
        monkeypatch.setattr(t, "ainvoke", fail_invoke)

    # mock _lookup_name 不调网络
    async def fake_lookup(code):
        return code
    monkeypatch.setattr("agents.nodes.decision._lookup_name", fake_lookup)

    state = {
        "messages": [{"role": "user", "content": "分析 600519"}],
        "context_codes": ["600519"],
    }
    result = await decision_node(state)
    assert result["decision_card"] is None
    assert result["intent"] == "decision_failed"
```

- [ ] **Step 2.2：跑测试验证失败**

```bash
cd /vol2/1000/code/stockclaw/backend
.venv/bin/python -m pytest tests/test_agents_decision.py::test_decision_node_returns_none_when_all_tools_fail -v
```

期望：FAIL —— `assert result["decision_card"] is None` 不成立（当前代码会生成全 0 卡）。

- [ ] **Step 2.3：改 decision_node 加 guard**

打开 `backend/agents/nodes/decision.py`，找到 `decision_node` 函数末尾的 `card = build_decision_card(...)` 那段，**在它之前**插入 guard：

```python
    # 若所有工具失败（current_price=0），不推半成品决策卡
    if not current_price:
        return {
            "decision_card": None,
            "intent": "decision_failed",
        }

    # 合并
    card = build_decision_card(
        code=code, name=name, current_price=current_price,
        ...
    )
    return {"decision_card": card}
```

完整上下文：找到 `current_price = (target_r or {}).get(...) or 0.0` 那行，下面就是 target_price/stop_loss 的 fallback 计算，然后是 `card = build_decision_card(...)`。把 guard 插在 fallback 计算之后、`build_decision_card` 之前。

- [ ] **Step 2.4：跑测试验证通过**

```bash
.venv/bin/python -m pytest tests/test_agents_decision.py::test_decision_node_returns_none_when_all_tools_fail -v
```

期望：PASS。

- [ ] **Step 2.5：跑全量回归**

```bash
.venv/bin/python -m pytest tests/test_agents_decision.py tests/test_runner.py tests/test_agent_endpoint.py -v 2>&1 | tail -10
```

期望：所有测试通过。如果 test_runner.py 的 `test_run_agent_emits_decision_artifact_for_decision_intent` 因此失败（因为它 mock 的 graph 返回 decision_card），那是 OK 的——它 mock 的卡片有 current_price=1685.0，不会被 guard 拦。

- [ ] **Step 2.6：commit**

```bash
cd /vol2/1000/code/stockclaw
git add backend/agents/nodes/decision.py backend/tests/test_agents_decision.py
git commit -m "fix(decision): 所有工具失败时拒推半成品决策卡

current_price=0（astock 网络挂 / code 找不到 / tencent_quote 空）时
不再生成全 0 决策卡。decision_node 返回 decision_card=None +
intent='decision_failed'，runner 据此不发 decision_artifact 事件。"
```

---

## Task 3：emit tool_trace 事件

**目标：** 当前用户看不到决策卡背后的工具调用过程。修法：decision_node 把每次工具调用的 `tool / status / args / summary` 收集到 state 里，runner 在推 decision_artifact 前先逐条 emit `tool_trace` 事件。

**Files:**
- Modify: `backend/agents/state.py`（加 `tool_traces` 字段）
- Modify: `backend/agents/nodes/decision.py`（收集 traces）
- Modify: `backend/runner.py`（emit 事件）
- Modify: `backend/tests/test_agents_decision.py`（加 1 个测试）
- Modify: `backend/tests/test_runner.py`（加 1 个测试）

- [ ] **Step 3.1：写失败的 decision_node 测试**

在 `backend/tests/test_agents_decision.py` 末尾追加：

```python
@pytest.mark.asyncio
async def test_decision_node_collects_tool_traces(monkeypatch):
    """decision_node 应把每次工具调用的 tool/status/args 收集到 state['tool_traces']。"""
    from agents.nodes.decision import decision_node, _invoke  # noqa: F401

    # mock forward_pe_target 成功，atr_stop 失败
    async def ok_target(tool, **kwargs):
        return {
            "tool": "forward_pe_target", "basis_type": "model",
            "model_version": "forward_pe_target.v1",
            "outputs": {"target_price": 1900.0, "current_price": 1685.0},
            "citations": [], "model_assumptions": [],
        }
    async def fail_stop(tool, **kwargs):
        return {
            "tool": "atr_stop", "basis_type": "model_fallback",
            "model_version": "atr_stop.v1",
            "outputs": {"fallback_reason": "tool_error"},
            "citations": [], "model_assumptions": [],
        }

    from agents.tools import forward_pe_target, atr_stop, pe_percentile_revert, risk_based_position, batch_build
    # 让前两个工具有不同的 mock，其他走 fail_invoke
    async def fail_invoke(tool, **kwargs):
        return {"tool": tool.name, "basis_type": "model_fallback",
                "model_version": f"{tool.name}.v1",
                "outputs": {"current_price": 1685.0, "stop_price": 1550.0},
                "citations": [], "model_assumptions": []}

    monkeypatch.setattr(forward_pe_target, "ainvoke", ok_target)
    monkeypatch.setattr(atr_stop, "ainvoke", fail_invoke)
    monkeypatch.setattr(pe_percentile_revert, "ainvoke", fail_invoke)
    monkeypatch.setattr(risk_based_position, "ainvoke", fail_invoke)
    monkeypatch.setattr(batch_build, "ainvoke", fail_invoke)

    async def fake_lookup(code):
        return "测试"
    monkeypatch.setattr("agents.nodes.decision._lookup_name", fake_lookup)

    state = {
        "messages": [{"role": "user", "content": "分析 600519"}],
        "context_codes": ["600519"],
    }
    result = await decision_node(state)
    traces = result.get("tool_traces") or []
    assert isinstance(traces, list)
    assert len(traces) == 5  # 5 个工具都被记录
    tools_called = [t["tool"] for t in traces]
    assert "forward_pe_target" in tools_called
    assert "atr_stop" in tools_called
    # 每条 trace 必须有 status 字段
    for t in traces:
        assert t["status"] in ("ok", "error")
```

- [ ] **Step 3.2：跑测试验证失败**

```bash
cd /vol2/1000/code/stockclaw/backend
.venv/bin/python -m pytest tests/test_agents_decision.py::test_decision_node_collects_tool_traces -v
```

期望：FAIL —— `result.get("tool_traces")` 为 None。

- [ ] **Step 3.3：扩 AgentState**

打开 `backend/agents/state.py`，在 `AgentState` TypedDict 里加一个字段：

```python
class AgentState(TypedDict, total=False):
    messages: Annotated[list, add_messages]
    intent: str
    context_codes: list[str]
    style: str
    artifacts: list[dict]
    thread_id: str
    decision_card: dict | None
    tool_traces: list[dict]   # ← 新增：[{tool, status, args, summary?}, ...]
```

- [ ] **Step 3.4：改 _invoke 收集 trace**

打开 `backend/agents/nodes/decision.py`，找到 `_invoke` 函数，改为返回 `(result, trace)` 元组：

```python
async def _invoke(tool, **kwargs) -> tuple[dict, dict]:
    """安全调用 tool，失败返回降级 dict。同时返回 trace 记录。"""
    trace = {
        "tool": tool.name,
        "status": "ok",
        "args": kwargs,
    }
    try:
        result = await tool.ainvoke(kwargs)
        # 加 summary：从前几个 outputs 字段拼一句话
        outs = (result or {}).get("outputs") or {}
        if "target_price" in outs:
            trace["summary"] = f"目标价 {outs['target_price']}"
        elif "stop_price" in outs:
            trace["summary"] = f"止损 {outs['stop_price']}"
        elif "shares" in outs:
            trace["summary"] = f"{outs['shares']:.0f} 股"
        return result, trace
    except Exception as e:
        trace["status"] = "error"
        trace["summary"] = f"{type(e).__name__}: {e}"
        return {
            "tool": tool.name, "error": f"{tool.name} failed: {e}",
            "basis_type": "model_fallback",
            "model_version": f"{tool.name}.v1",
            "outputs": {"fallback_reason": f"tool_error: {type(e).__name__}"},
            "citations": [], "model_assumptions": [f"工具失败：{e}"],
        }, trace
```

- [ ] **Step 3.5：改 decision_node 解包 tuple**

在同一个文件 `decision_node` 函数里，找到所有 `_invoke(...)` 调用，全部改为解包：

```python
    target_r, target_t = await _invoke(forward_pe_target, code=code, target_pe=20.0, eps_year="27e")
    stop_r, stop_t = await _invoke(atr_stop, code=code, period=14, multiplier=2.0)
    entry_r, entry_t = await _invoke(pe_percentile_revert, code=code, revert_to=0.50)
    # ...
    pos_r, pos_t = await _invoke(risk_based_position, entry_price=current_price or 100.0, stop_price=stop_loss or 92.0)
    cad_r, cad_t = await _invoke(batch_build, total_budget=100000.0, batches=3, schedule="weekly", start_price=current_price or 100.0)
```

然后在函数末尾 `return {"decision_card": card}` 改为：

```python
    tool_traces = [target_t, stop_t, entry_t, pos_t, cad_t]
    return {"decision_card": card, "tool_traces": tool_traces}
```

注意：guard 路径（Task 2 加的 `if not current_price: return {...}`）也要带上 traces：

```python
    if not current_price:
        return {
            "decision_card": None,
            "intent": "decision_failed",
            "tool_traces": [target_t, stop_t, entry_t],
        }
```

- [ ] **Step 3.6：跑测试验证通过**

```bash
.venv/bin/python -m pytest tests/test_agents_decision.py::test_decision_node_collects_tool_traces -v
```

期望：PASS。

- [ ] **Step 3.7：runner 加 tool_trace 事件 emit**

打开 `backend/runner.py`，找到 `run_agent` 函数。当前结构（Task 4 会重写顺序，但先在原顺序上加 emit）：

```python
        graph_result = await agent_graph.ainvoke(graph_state)
        decision_card = graph_result.get("decision_card")
```

在 `decision_card = ...` 之后、`async for text in _stream_llm_text(...)` 之前，插入：

```python
        # 推 tool_trace 事件（decision 路径才有）
        for trace in (graph_result.get("tool_traces") or []):
            yield {"type": "tool_trace", **trace}
```

- [ ] **Step 3.8：写 runner 测试**

打开 `backend/tests/test_runner.py`，在 `test_run_agent_emits_decision_artifact_for_decision_intent` 里扩展断言。找到现有 `types = [e["type"] for e in events]` 那行，后面加：

```python
    # 至少 5 个 tool_trace（forward_pe_target + atr_stop + pe_percentile_revert + risk_based_position + batch_build）
    tool_traces = [e for e in events if e["type"] == "tool_trace"]
    assert len(tool_traces) >= 1, f"expected tool_trace events, got {events}"
```

为了让这个断言通过，需要修改 `fake_graph_ainvoke` 让它返回 `tool_traces` 字段：

```python
    async def fake_graph_ainvoke(input_state):
        return {
            "intent": "decision",
            "decision_card": {
                "code": "600519", "name": "茅台", "current_price": 1685.0,
                "target_price": 1900.0, "entry_low": 1685.0, "entry_high": 1720.0,
                "stop_loss": 1550.0, "take_profit": 2080.0, "cadence": [],
                "basis_type": "model", "model_versions_json": {},
                "assumptions": [], "citations": [], "explanation": "测试"
            },
            "tool_traces": [
                {"tool": "forward_pe_target", "status": "ok", "args": {"code": "600519"}},
                {"tool": "atr_stop", "status": "ok", "args": {"code": "600519"}},
            ],
        }
```

- [ ] **Step 3.9：跑 runner 测试**

```bash
.venv/bin/python -m pytest tests/test_runner.py -v
```

期望：两个测试都 PASS。

- [ ] **Step 3.10：跑全量回归**

```bash
.venv/bin/python -m pytest -m "not live" --ignore=tests/test_api.py -v 2>&1 | tail -5
```

期望：61 passed（Phase 1 的 60 + 本 task 新增 1）。

- [ ] **Step 3.11：commit**

```bash
cd /vol2/1000/code/stockclaw
git add backend/agents/state.py backend/agents/nodes/decision.py backend/runner.py backend/tests/test_agents_decision.py backend/tests/test_runner.py
git commit -m "feat(agent): emit tool_trace NDJSON 事件

- AgentState 加 tool_traces 字段
- _invoke 改返回 (result, trace) 元组，trace 含 tool/status/args/summary
- decision_node 收集 5 条 trace 写回 state
- runner 在 decision_artifact 之前逐条 emit tool_trace 事件
- 用户能看到 \"atr_stop 运行中 → ok · 止损 1550\" 等折叠小药丸"
```

---

## Task 4：并发跑 graph 与 LLM（消除 5s 首字延迟）

**目标：** 当前 `run_agent` 顺序：graph（5 工具 × 1s cool-down = 5s）→ 流 LLM。用户等 5s 才看到首字。改并发：graph 在后台跑，立刻开始流 LLM；graph 完成后推 tool_trace + decision_artifact。

**两个明确 UX 妥协（评审 #1 + #3）：**

1. **LLM 失去工具上下文**：并发后 LLM 拿不到 `[工具结果摘要]` 注入。可以接受——决策卡自己有结构化数字，LLM 文本只负责通用解读。
2. **tool_trace 非实时**：traces 在 graph 完成后（也就是 LLM 流完后或同时）一次性 emit，不是每个工具 running→ok 实时推送。要做真正实时需要 graph 改 `astream_events` + `asyncio.Queue`，属于 Phase 2 范畴。Phase 1.5 接受这个妥协。

**新增 error 事件（评审 #1）：** 当 Task 2 的 guard 触发（`intent == "decision_failed"`），runner 必须发 `error` 事件而不是静默 `done`。

**Files:**
- Modify: `backend/runner.py`
- Modify: `backend/tests/test_runner.py`

- [ ] **Step 4.1：写失败的并发测试**

在 `backend/tests/test_runner.py` 加新测试：

```python
@pytest.mark.asyncio
async def test_run_agent_streams_llm_before_graph_finishes(monkeypatch):
    """LLM 流必须在 graph 完成前就开始——验证并发。"""
    import asyncio
    graph_done_time = []
    llm_start_time = []

    async def slow_graph(input_state):
        await asyncio.sleep(0.5)  # 模拟 5 工具 × 0.1s
        graph_done_time.append(asyncio.get_event_loop().time())
        return {"intent": "decision", "decision_card": None, "tool_traces": []}

    async def fake_stream(*args, **kwargs):
        llm_start_time.append(asyncio.get_event_loop().time())
        yield "首字"

    _patch_httpx_stream(monkeypatch, [_sse_delta("首字"), "data: [DONE]"])
    monkeypatch.setattr("runner.agent_graph.ainvoke", slow_graph)
    monkeypatch.setattr("runner._stream_llm_text", fake_stream)

    req = runner.AgentChatReq(
        thread_id=None,
        messages=[{"role": "user", "content": "分析"}],
        llm={"provider": "", "baseURL": "http://x", "apiKey": "k", "model": "m"},
    )
    events = []
    async for ev in runner.run_agent(req):
        events.append(ev)

    # LLM 必须在 graph 完成前开始
    assert llm_start_time and graph_done_time
    assert llm_start_time[0] < graph_done_time[0], "LLM 应在 graph 完成前开始流式"
```

- [ ] **Step 4.2：跑测试验证失败**

```bash
cd /vol2/1000/code/stockclaw/backend
.venv/bin/python -m pytest tests/test_runner.py::test_run_agent_streams_llm_before_graph_finishes -v
```

期望：FAIL —— `llm_start_time[0] < graph_done_time[0]` 不成立（当前顺序执行）。

- [ ] **Step 4.3：改 run_agent 并发**

打开 `backend/runner.py`，找到 `run_agent` 函数。当前完整结构：

```python
async def run_agent(req: AgentChatReq) -> AsyncGenerator[dict, None]:
    decision_id = uuid.uuid4().hex
    try:
        graph_state = {...}
        graph_result = await agent_graph.ainvoke(graph_state)   # ← 阻塞 5s
        decision_card = graph_result.get("decision_card")
        # ... tool_trace emit ...
        async for text in _stream_llm_text(...):
            yield {"type": "text_delta", "text": text}
        if decision_card:
            yield {"type": "decision_artifact", ...}
        yield {"type": "done", ...}
    except Exception as e:
        yield {"type": "error", ...}
```

替换为并发版：

```python
async def run_agent(req: AgentChatReq) -> AsyncGenerator[dict, None]:
    decision_id = uuid.uuid4().hex
    try:
        graph_state = {
            "messages": req.messages,
            "context_codes": req.context_codes,
            "style": req.style,
            "thread_id": req.thread_id or decision_id,
        }

        # 并发：graph 在后台跑，立刻开始流 LLM
        graph_task = asyncio.create_task(agent_graph.ainvoke(graph_state))

        # LLM 流式（不依赖 graph 结果——决策卡自己有结构化数字）
        async for text in _stream_llm_text(
            req.llm.model_dump(), SYSTEM_PROMPT_AGENT, req.messages, req.context_codes,
        ):
            yield {"type": "text_delta", "text": text}

        # 等 graph 完成（如果还没好）
        graph_result = await graph_task
        decision_card = graph_result.get("decision_card")
        graph_intent = graph_result.get("intent")

        # 评审 #1：工具全失败时显式发 error，不静默 done
        if graph_intent == "decision_failed":
            yield {
                "type": "error",
                "message": "获取股票数据失败，无法生成决策卡。请检查代码或稍后重试。",
            }
            yield {"type": "done", "summary": {"thread_id": req.thread_id or decision_id, "failed": True}}
            return

        # 推 tool_trace 事件（decision 路径才有；非实时——一次性 dump）
        for trace in (graph_result.get("tool_traces") or []):
            yield {"type": "tool_trace", **trace}

        # 推决策卡 artifact
        if decision_card:
            yield {
                "type": "decision_artifact",
                "decision_id": decision_id,
                "data": decision_card,
            }
            yield {
                "type": "citations",
                "items": decision_card.get("citations") or [],
            }

        yield {"type": "done", "summary": {"thread_id": req.thread_id or decision_id}}

    except Exception as e:
        yield {"type": "error", "message": f"agent 运行失败：{e}"}
```

注意 3 个改动：
1. 加 `import asyncio`（如果还没有）
2. `asyncio.create_task` 启动 graph 后台
3. 删掉 `enhanced_messages` 那段（不再注入工具摘要——并发拿不到）

- [ ] **Step 4.4：删掉/更新老测试里依赖工具摘要注入的逻辑**

`test_runner.py::test_run_agent_emits_decision_artifact_for_decision_intent` 之前 mock 了 `_stream_llm_text`，本 task 改完后还在用 `_patch_httpx_stream`。但 Step 4.3 删了 enhanced_messages 的逻辑，老测试用 `_patch_httpx_stream(monkeypatch, sse_lines)` 走的是 httpx mock，依然有效。

确保 `import asyncio` 在 runner.py 顶部。

- [ ] **Step 4.4b：加 decision_failed 路径的 error 事件测试（评审 #1）**

在 `test_runner.py` 加：

```python
@pytest.mark.asyncio
async def test_run_agent_emits_error_when_decision_fails(monkeypatch):
    """Task 2 的 guard 触发时（intent=decision_failed），runner 必须发 error 事件。"""
    async def failing_graph(input_state):
        return {
            "intent": "decision_failed",
            "decision_card": None,
            "tool_traces": [
                {"tool": "forward_pe_target", "status": "error", "args": {}},
            ],
        }

    _patch_httpx_stream(monkeypatch, [_sse_delta("分析中..."), "data: [DONE]"])
    monkeypatch.setattr("runner.agent_graph.ainvoke", failing_graph)

    req = runner.AgentChatReq(
        thread_id=None,
        messages=[{"role": "user", "content": "分析 XXXXXX"}],
        llm={"provider": "", "baseURL": "http://x", "apiKey": "k", "model": "m"},
    )
    events = []
    async for ev in runner.run_agent(req):
        events.append(ev)

    types = [e["type"] for e in events]
    assert "error" in types, f"决策失败应发 error 事件，实际 {types}"
    err = next(e for e in events if e["type"] == "error")
    assert "数据失败" in err["message"] or "无法生成" in err["message"]
    # 失败时仍应推 tool_trace（让用户看到哪些工具挂了）
    assert "tool_trace" in types
    # done 必须最后
    assert types[-1] == "done"
```

- [ ] **Step 4.5：跑 runner 测试**

```bash
.venv/bin/python -m pytest tests/test_runner.py -v
```

期望：4 个测试全 PASS（包括新的并发测试 + decision_failed error 测试）。

- [ ] **Step 4.6：跑全量回归**

```bash
.venv/bin/python -m pytest -m "not live" --ignore=tests/test_api.py -v 2>&1 | tail -5
```

期望：63 passed（60 Phase 1 + Task 3 新增 1 + Task 4 新增 2）。

- [ ] **Step 4.7：commit**

```bash
cd /vol2/1000/code/stockclaw
git add backend/runner.py backend/tests/test_runner.py
git commit -m "perf(runner): 并发跑 graph 与 LLM，消除 5s 首字延迟

之前 run_agent 顺序：graph.ainvoke (5s) → stream LLM。
用户等 5s 才看到首字。

改 asyncio.create_task(graph) 后台跑，立刻开始流 LLM。
graph 完成后再 emit tool_trace + decision_artifact。

代价：LLM 不再能拿到 \"[工具结果摘要]\" 上下文注入（并发拿不到）。
可以接受——决策卡本身有结构化数字，LLM 文本负责解读。"
```

---

## Task 5：threads + messages HTTP CRUD 端点

**目标：** Phase 1 后端已有 `persistence/{threads,conversations,decisions}.py`，但没有 HTTP 端点暴露。前端没法拉历史会话。本 task 加 5 个端点。

**Files:**
- Modify: `backend/app.py`（加 5 个路由 + 2 个 Pydantic 模型）
- Create: `backend/tests/test_agent_threads_api.py`

- [ ] **Step 5.1：写失败的端点测试**

创建 `backend/tests/test_agent_threads_api.py`：

```python
"""/api/agent/threads* CRUD 端点协议测试。"""
import asyncio

import pytest
from fastapi.testclient import TestClient


@pytest.fixture
def client(monkeypatch, tmp_path):
    """每个测试一个临时 db（用 pytest tmp_path，避免 tempfile.mktemp 弃用警告）。"""
    monkeypatch.delenv("VR_API_KEY", raising=False)
    db_path = tmp_path / "test.db"
    monkeypatch.setenv("VR_AGENT_DB", str(db_path))
    import app
    from persistence import db
    asyncio.run(db.close_db())
    asyncio.run(db.init_db())
    return TestClient(app.app)


def test_list_threads_empty(client):
    """空 DB 时 GET /api/agent/threads 返回 []。"""
    r = client.get("/api/agent/threads")
    assert r.status_code == 200
    assert r.json() == []


def test_create_thread_returns_id(client):
    """POST /api/agent/threads 返回新 thread_id + 元数据。"""
    r = client.post("/api/agent/threads", json={"title": "测试", "model": "glm-4.6"})
    assert r.status_code == 200
    body = r.json()
    assert "id" in body
    assert body["title"] == "测试"
    assert body["model"] == "glm-4.6"
    assert "created_at" in body


def test_create_thread_accepts_client_id(client):
    """前端可选用 crypto.randomUUID 提供 id，省去 local-xxx → real-xxx 映射。"""
    r = client.post("/api/agent/threads", json={
        "id": "client-uuid-1234",
        "title": "前端生成 ID",
        "model": "m",
    })
    assert r.status_code == 200
    assert r.json()["id"] == "client-uuid-1234"


def test_list_threads_after_create(client):
    """创建后 list 能看到。"""
    client.post("/api/agent/threads", json={"title": "A", "model": "m"})
    client.post("/api/agent/threads", json={"title": "B", "model": "m"})
    r = client.get("/api/agent/threads")
    assert r.status_code == 200
    items = r.json()
    assert len(items) == 2
    # 按 updated_at 倒序：B 应在前
    titles = [t["title"] for t in items]
    assert titles == ["B", "A"]


def test_rename_thread(client):
    """PATCH /api/agent/threads/:id 改标题。"""
    r = client.post("/api/agent/threads", json={"title": "old", "model": "m"})
    tid = r.json()["id"]
    r2 = client.patch(f"/api/agent/threads/{tid}", json={"title": "new"})
    assert r2.status_code == 200
    assert r2.json() == {"ok": True}


def test_delete_thread(client):
    """DELETE /api/agent/threads/:id 删除 + CASCADE 清消息。"""
    r = client.post("/api/agent/threads", json={"title": "x", "model": "m"})
    tid = r.json()["id"]
    r2 = client.delete(f"/api/agent/threads/{tid}")
    assert r2.status_code == 200
    assert r2.json() == {"ok": True}
    # list 里没了
    assert all(t["id"] != tid for t in client.get("/api/agent/threads").json())


def test_list_messages_empty(client):
    """空 thread GET messages 返回 []。"""
    r = client.post("/api/agent/threads", json={"title": "x", "model": "m"})
    tid = r.json()["id"]
    r2 = client.get(f"/api/agent/threads/{tid}/messages")
    assert r2.status_code == 200
    assert r2.json() == []
```

- [ ] **Step 5.2：跑测试验证失败**

```bash
cd /vol2/1000/code/stockclaw/backend
.venv/bin/python -m pytest tests/test_agent_threads_api.py -v
```

期望：所有 6 个测试 FAIL（路由 404）。

- [ ] **Step 5.3：在 app.py 加 Pydantic 模型**

打开 `backend/app.py`，找到已有的 `from runner import AgentChatReq, run_agent` 这一行附近。在它下面加：

```python
class ThreadCreateReq(BaseModel):
    id: str | None = None       # 前端可选 crypto.randomUUID() 提供，省去 local-xxx → real-xxx 映射
    title: str = "新会话"
    model: str = ""


class ThreadRenameReq(BaseModel):
    title: str


class AppendMessageReq(BaseModel):
    role: str = "user"
    content: str = ""
```

- [ ] **Step 5.4：在 app.py 加路由**

找到 `/api/agent/chat` 路由定义之后，追加 6 个路由：

```python
@app.get("/api/agent/threads")
async def agent_list_threads():
    """列出所有会话，按 updated_at 倒序。"""
    from persistence import threads
    return await threads.list_threads()


@app.post("/api/agent/threads")
async def agent_create_thread(req: ThreadCreateReq):
    """新建会话；前端可选提供 id（crypto.randomUUID）。
    返回完整 thread 对象。"""
    from persistence import threads
    tid = await threads.create_thread(title=req.title, model=req.model, tid=req.id)
    return await threads.get_thread(tid)


@app.patch("/api/agent/threads/{tid}")
async def agent_rename_thread(tid: str, req: ThreadRenameReq):
    from persistence import threads
    await threads.rename_thread(tid, req.title)
    return {"ok": True}


@app.delete("/api/agent/threads/{tid}")
async def agent_delete_thread(tid: str):
    from persistence import threads
    await threads.delete_thread(tid)
    return {"ok": True}


@app.get("/api/agent/threads/{tid}/messages")
async def agent_list_messages(tid: str):
    from persistence import conversations
    return await conversations.list_messages(tid)


@app.post("/api/agent/threads/{tid}/messages")
async def agent_append_message(tid: str, req: AppendMessageReq):
    """归档一条消息（user 或 assistant）。"""
    from persistence import conversations
    mid = await conversations.append_message(tid, {"role": req.role, "content": req.content})
    return {"id": mid}
```

注：原 Task 6 Step 6.5 的 POST messages 端点合并到这里，避免任务交叉。

- [ ] **Step 5.5：跑测试验证通过**

```bash
.venv/bin/python -m pytest tests/test_agent_threads_api.py -v
```

期望：6 个测试全 PASS。

- [ ] **Step 5.6：跑全量回归**

```bash
.venv/bin/python -m pytest -m "not live" --ignore=tests/test_api.py -v 2>&1 | tail -5
```

期望：68 passed（62 + 6）。

- [ ] **Step 5.7：commit**

```bash
cd /vol2/1000/code/stockclaw
git add backend/app.py backend/tests/test_agent_threads_api.py
git commit -m "feat(api): /api/agent/threads CRUD + messages 列表端点

- GET    /api/agent/threads            列表（updated_at 倒序）
- POST   /api/agent/threads            新建
- PATCH  /api/agent/threads/:id        改标题
- DELETE /api/agent/threads/:id        删（CASCADE 清消息）
- GET    /api/agent/threads/:id/messages  历史消息

复用 Phase 1 已建的 persistence/{threads,conversations}.py。"
```

---

## Task 6：前端会话列表全链路（拉取/创建/归档消息）

**目标：** AgentSidebar 在 mount 时拉历史列表；用户首次发消息自动 POST 创建 thread；流式结束后归档 user/assistant 消息到后端。刷新 `/agent` 仍能看到历史。

**Files:**
- Modify: `frontend/src/lib/stores/agent.ts`（加 `loadThreads` / `addLocalThread` actions）
- Modify: `frontend/src/components/agent/AgentSidebar.tsx`（onMount fetch + 重命名/删除按钮）
- Modify: `frontend/src/hooks/useAgentStream.ts`（无 threadId 时先 POST；done 后归档）
- Modify: `frontend/src/lib/api.ts`（校对 agentApi 签名匹配后端）

- [ ] **Step 6.1：校对 api.ts 的 agentApi**

打开 `frontend/src/lib/api.ts`，找到 `agentApi` 块（Phase 1 Task 10 加的）。检查方法签名是否匹配 Task 5 的后端：

```typescript
export const agentApi = {
  listThreads: () => get<AgentThread[]>("/agent/threads"),
  createThread: (title: string, model: string) =>
    request<AgentThread>("/agent/threads", "POST", { title, model }),
  renameThread: (tid: string, title: string) =>
    request<{ ok: boolean }>(`/agent/threads/${tid}`, "PATCH", { title }),
  deleteThread: (tid: string) =>
    request<{ ok: boolean }>(`/agent/threads/${tid}`, "DELETE"),
  listMessages: (tid: string) => get<unknown[]>(`/agent/agent/threads/${tid}/messages`),
  // ...
};
```

如果 `listMessages` 路径错了（双 `/agent/agent`），修正为 `/agent/threads/${tid}/messages`：

```typescript
  listMessages: (tid: string) => get<unknown[]>(`/agent/threads/${tid}/messages`),
```

- [ ] **Step 6.2：store 加 loadThreads action**

打开 `frontend/src/lib/stores/agent.ts`，在 `AgentState` interface 加：

```typescript
  loadThreads: (threads: AgentThread[]) => void;
```

在 create 实现里加：

```typescript
  loadThreads: (threads) => set({ threads }),
```

- [ ] **Step 6.3：AgentSidebar 改造**

打开 `frontend/src/components/agent/AgentSidebar.tsx`，全文替换：

```tsx
import { useEffect, useState } from "react";
import { Plus, MessageSquare, Trash2, Pencil, Check, X } from "lucide-react";
import { useAgentStore } from "@/lib/stores/agent";
import { agentApi } from "@/lib/api";
import { cn } from "@/lib/utils";
import type { AgentThread } from "@/lib/types/agent";

export function AgentSidebar() {
  const threads = useAgentStore((s) => s.threads);
  const currentThreadId = useAgentStore((s) => s.currentThreadId);
  const setCurrentThread = useAgentStore((s) => s.setCurrentThread);
  const loadThreads = useAgentStore((s) => s.loadThreads);
  const [editingId, setEditingId] = useState<string | null>(null);
  const [draftTitle, setDraftTitle] = useState("");

  // mount 时拉历史会话
  useEffect(() => {
    agentApi.listThreads().then((items: AgentThread[]) => loadThreads(items)).catch(() => {});
  }, [loadThreads]);

  const newThread = () => {
    // 本地先建（不调后端，等用户真发消息时由 useAgentStream 创建）
    const tid = `local-${Date.now()}`;
    loadThreads([
      { id: tid, title: "新会话", model: "", created_at: Date.now(), updated_at: Date.now() },
      ...useAgentStore.getState().threads,
    ]);
    setCurrentThread(tid);
    useAgentStore.setState((s) => ({
      messagesByThread: { ...s.messagesByThread, [tid]: [] },
    }));
  };

  const commitRename = async (tid: string) => {
    const t = draftTitle.trim();
    if (!t) { setEditingId(null); return; }
    // 本地先改
    loadThreads(useAgentStore.getState().threads.map((x) => x.id === tid ? { ...x, title: t } : x));
    setEditingId(null);
    // 后端同步（local- 前缀的还没建到后端，跳过）
    if (!tid.startsWith("local-")) {
      try { await agentApi.renameThread(tid, t); } catch {}
    }
  };

  const remove = async (tid: string) => {
    loadThreads(useAgentStore.getState().threads.filter((x) => x.id !== tid));
    if (!tid.startsWith("local-")) {
      try { await agentApi.deleteThread(tid); } catch {}
    }
  };

  return (
    <aside className="glass flex w-60 flex-col rounded-2xl">
      <button
        onClick={newThread}
        className="m-2 flex items-center justify-center gap-2 rounded-lg bg-primary/10 px-3 py-2 text-sm font-medium text-primary hover:bg-primary/20"
      >
        <Plus className="h-4 w-4" /> 新建会话
      </button>
      <div className="flex-1 overflow-auto p-2">
        {threads.length === 0 && (
          <p className="px-2 py-4 text-xs text-muted-foreground">暂无会话</p>
        )}
        {threads.map((t) => (
          <div
            key={t.id}
            className={cn(
              "mb-1 flex w-full items-center gap-2 rounded-lg px-2.5 py-2 text-sm transition-colors group",
              t.id === currentThreadId
                ? "bg-primary/15 font-medium text-primary"
                : "text-muted-foreground hover:bg-muted/50 hover:text-foreground",
            )}
          >
            <MessageSquare className="h-3.5 w-3.5 shrink-0" />
            {editingId === t.id ? (
              <>
                <input
                  value={draftTitle}
                  onChange={(e) => setDraftTitle(e.target.value)}
                  onKeyDown={(e) => { if (e.key === "Enter") commitRename(t.id); if (e.key === "Escape") setEditingId(null); }}
                  className="flex-1 bg-transparent text-xs outline-none"
                  autoFocus
                />
                <button onClick={() => commitRename(t.id)} className="text-emerald-500"><Check className="h-3 w-3" /></button>
                <button onClick={() => setEditingId(null)} className="text-muted-foreground"><X className="h-3 w-3" /></button>
              </>
            ) : (
              <>
                <button onClick={() => setCurrentThread(t.id)} className="flex-1 truncate text-left">
                  {t.title}
                </button>
                <button
                  onClick={() => { setEditingId(t.id); setDraftTitle(t.title); }}
                  className="hidden group-hover:block text-muted-foreground hover:text-foreground"
                  title="重命名"
                >
                  <Pencil className="h-3 w-3" />
                </button>
                <button
                  onClick={() => remove(t.id)}
                  className="hidden group-hover:block text-muted-foreground hover:text-red-500"
                  title="删除"
                >
                  <Trash2 className="h-3 w-3" />
                </button>
              </>
            )}
          </div>
        ))}
      </div>
    </aside>
  );
}
```

- [ ] **Step 6.4：useAgentStream 改造（创建 + 归档）**

打开 `frontend/src/hooks/useAgentStream.ts`，找到 `send` 函数。**评审 #6 简化**：前端用 `crypto.randomUUID()` 直接生成 thread id，POST 时带上，后端原样接收——不再需要 `local-xxx → real-xxx` 映射。

在创建 placeholder assistant 消息之前插入：

```typescript
    // 评审 #6：前端直接 crypto.randomUUID 生成 tid，免 local-xxx → real-xxx 映射
    let tid = opts.threadId;
    if (!tid) {
      tid = (globalThis.crypto?.randomUUID?.() ?? `local-${Date.now()}`);
      const title = opts.content.slice(0, 30) + (opts.content.length > 30 ? "..." : "");
      // 本地 store 先建
      useAgentStore.setState((s) => ({
        threads: [
          { id: tid!, title, model: "", created_at: Date.now(), updated_at: Date.now() },
          ...s.threads,
        ],
        currentThreadId: tid,
        messagesByThread: { ...s.messagesByThread, [tid!]: [] },
      }));
      // 后端建（带 id 字段，让后端用我们的 tid）
      try {
        await agentApi.createThread(title, "", tid);
      } catch (e) {
        console.error("createThread 失败，降级本地：", e);
      }
    }

    // 评审 #4：user 消息必须**先于** SSE fetch 归档——浏览器关闭/网络断时不丢
    try {
      await agentApi.saveMessage(tid, { role: "user", content: opts.content });
    } catch (e) {
      console.error("user 消息归档失败：", e);
    }
```

需要从 api.ts 引入：

```typescript
import { agentApi, authHeaders } from "@/lib/api";
```

并把 `const tid = ...` 改成 `let tid = ...`。

在 `finally` 块开头只归档 assistant 消息（user 已在 fetch 前归档）：

```typescript
    } finally {
      // 只归档 assistant 消息（user 在 fetch 前已归档）
      try {
        const finalContent = useAgentStore.getState().messagesByThread[tid!]?.find((m) => m.id === assistantMsgId)?.content || "";
        if (finalContent) {
          await agentApi.saveMessage(tid, { role: "assistant", content: finalContent });
        }
      } catch (e) {
        console.error("assistant 归档失败：", e);
      }
      useAgentStore.getState().finishStreaming(tid, assistantMsgId);
      // ... 原 streaming.active=false ...
    }
```

注意：fetch 时 `body.thread_id` 必须用这个 `tid`（让 Task 7 的 decisions 外键能对上）：

```typescript
    const body = {
      thread_id: tid,
      messages: [{ role: "user", content: opts.content }],
      context_codes: opts.contextCodes,
      llm,
      style: opts.style,
    };
```

- [ ] **Step 6.5：api.ts 校对（已有方法，调整签名）**

打开 `frontend/src/lib/api.ts`，在 `agentApi` 里把 `createThread` 改成接受可选第三参数 `id`：

```typescript
  createThread: (title: string, model: string, id?: string) =>
    request<AgentThread>("/agent/threads", "POST", id ? { id, title, model } : { title, model }),

  saveMessage: (tid: string, message: { role: string; content: string }) =>
    request<{ id: string }>(`/agent/threads/${tid}/messages`, "POST", message),
```

- [ ] **Step 6.6：前端 build 验证**

```bash
cd /vol2/1000/code/stockclaw/frontend
npm run build 2>&1 | tail -5
```

期望：build 成功无 TS 报错。

- [ ] **Step 6.7：手动验证**

```bash
cd backend && .venv/bin/python -m uvicorn app:app --port 8900 --reload &
cd ../frontend && npm run dev &
```

浏览器 `/agent`：
1. 刷新 → sidebar 空（首次）
2. 输入「你好」→ 发送 → 流式回答
3. sidebar 立刻出现新会话「你好」（标题来自首消息前 30 字）
4. 关闭浏览器 → 重新打开 → 会话 + 消息都在
5. 鼠标悬停会话 → 见重命名/删除按钮
6. **断网测试**：拔网线后发消息 → 流式失败 → 但 user 消息已归档（Network 标签可见先 POST messages 成功，再 SSE 失败）

- [ ] **Step 6.8：commit**

```bash
cd /vol2/1000/code/stockclaw
git add backend/app.py frontend/src/lib/api.ts frontend/src/lib/stores/agent.ts frontend/src/components/agent/AgentSidebar.tsx frontend/src/hooks/useAgentStream.ts
git commit -m "feat(agent): 会话列表全链路持久化

- AgentSidebar: onMount 拉历史会话；hover 显示重命名/删除按钮
- useAgentStream: crypto.randomUUID 生成 tid 直接给后端（评审 #6 简化）
- user 消息在 SSE fetch 前归档（评审 #4，断网/关 tab 不丢）
- assistant 消息在 finally 块归档（流式完成或中断都尝试）
- POST /api/agent/threads/:id/messages 端点（Task 5 已加）
- store 加 loadThreads action
- 刷新 /agent 仍能看到历史会话与消息"
```

---

## Task 7：decision_artifact 入库（runner 完成后写 decisions 表）

**目标：** Phase 3 收益追踪依赖 decisions 表的 `status='active'` 记录。目前 runner emit decision_artifact 后不入库。修法：emit 完后调 `decisions.create_decision(...)`。

**Files:**
- Modify: `backend/runner.py`
- Modify: `backend/tests/test_runner.py`

- [ ] **Step 7.1：写失败的入库测试**

在 `backend/tests/test_runner.py` 加：

```python
@pytest.mark.asyncio
async def test_run_agent_persists_decision_card(monkeypatch, tmp_path):
    """decision_artifact emit 后应写一条 decisions 记录。"""
    async def fake_graph_ainvoke(input_state):
        return {"intent": "decision", "decision_card": {
            "code": "600519", "name": "茅台", "current_price": 1685.0,
            "target_price": 1900.0, "entry_low": 1685.0, "entry_high": 1720.0,
            "stop_loss": 1550.0, "take_profit": 2080.0,
            "cadence": [{"batch": 1, "pct": 1.0, "trigger": "immediate"}],
            "basis_type": "model", "model_versions_json": {},
            "assumptions": [], "citations": [], "explanation": "测试"
        }, "tool_traces": []}

    _patch_httpx_stream(monkeypatch, [_sse_delta("ok"), "data: [DONE]"])
    monkeypatch.setattr("runner.agent_graph.ainvoke", fake_graph_ainvoke)

    # 评审 #5：用 pytest tmp_path 而非 tempfile.mktemp
    db_path = tmp_path / "test.db"
    monkeypatch.setenv("VR_AGENT_DB", str(db_path))
    from persistence import db, threads
    await db.close_db()
    await db.init_db()
    # 评审 #2：先建 thread 行，避免 decisions 外键违反
    await threads.create_thread(tid="test-thread", title="t", model="m")

    req = runner.AgentChatReq(
        thread_id="test-thread",
        messages=[{"role": "user", "content": "分析 600519"}],
        llm={"provider": "", "baseURL": "http://x", "apiKey": "k", "model": "m"},
    )
    events = []
    async for ev in runner.run_agent(req):
        events.append(ev)

    # 检查 decisions 表里有一条记录
    from persistence import decisions
    items = await decisions.list_by_code("600519")
    assert len(items) >= 1
    assert items[0]["target_price"] == 1900.0
    assert items[0]["status"] == "active"
    assert items[0]["thread_id"] == "test-thread"
    await db.close_db()


@pytest.mark.asyncio
async def test_run_agent_creates_thread_when_none_provided(monkeypatch, tmp_path):
    """评审 #2：req.thread_id 为 None 时 runner 应自动建 thread，不能 IntegrityError。"""
    async def fake_graph_ainvoke(input_state):
        return {"intent": "decision", "decision_card": {
            "code": "000001", "name": "平安银行", "current_price": 12.0,
            "target_price": 14.0, "entry_low": 12.0, "entry_high": 12.3,
            "stop_loss": 11.0, "take_profit": 16.0,
            "cadence": [], "basis_type": "model", "model_versions_json": {},
            "assumptions": [], "citations": [], "explanation": "测试"
        }, "tool_traces": []}

    _patch_httpx_stream(monkeypatch, [_sse_delta("ok"), "data: [DONE]"])
    monkeypatch.setattr("runner.agent_graph.ainvoke", fake_graph_ainvoke)

    db_path = tmp_path / "test.db"
    monkeypatch.setenv("VR_AGENT_DB", str(db_path))
    from persistence import db, threads
    await db.close_db()
    await db.init_db()

    req = runner.AgentChatReq(
        thread_id=None,  # ← 关键：不传 thread_id
        messages=[{"role": "user", "content": "分析 000001"}],
        llm={"provider": "", "baseURL": "http://x", "apiKey": "k", "model": "m"},
    )
    events = []
    async for ev in runner.run_agent(req):
        events.append(ev)

    # 不应崩；decisions 表应有一条记录，且 thread_id 指向新建的 thread
    from persistence import decisions, threads as threads_dao
    items = await decisions.list_by_code("000001")
    assert len(items) == 1
    new_tid = items[0]["thread_id"]
    assert await threads_dao.get_thread(new_tid) is not None  # thread 行存在
    await db.close_db()
```

- [ ] **Step 7.2：跑测试验证失败**

```bash
cd /vol2/1000/code/stockclaw/backend
.venv/bin/python -m pytest tests/test_runner.py::test_run_agent_persists_decision_card -v
```

期望：FAIL —— `assert len(items) >= 1` 不成立（当前不入库）。

- [ ] **Step 7.3：runner 加 decisions 入库**

打开 `backend/runner.py`，找到 `if decision_card:` 块（emit decision_artifact + citations 那段）。在 `yield {"type": "citations", ...}` 之后加：

```python
        if decision_card:
            yield {
                "type": "decision_artifact",
                "decision_id": decision_id,
                "data": decision_card,
            }
            yield {
                "type": "citations",
                "items": decision_card.get("citations") or [],
            }
            # 入库 decisions 表（Phase 3 收益追踪用）
            try:
                from persistence import decisions, threads as threads_dao
                # 评审 #2：req.thread_id 为空时先建 thread 行，避免 FK 违反
                effective_tid = req.thread_id
                if not effective_tid:
                    effective_tid = await threads_dao.create_thread(
                        title=f"决策 · {decision_card.get('code', '')}",
                        model=req.llm.model,
                    )
                else:
                    # 已传 thread_id 但可能不在 threads 表（前端没先建）→ 兜底建
                    if not await threads_dao.get_thread(effective_tid):
                        await threads_dao.create_thread(
                            tid=effective_tid,
                            title=f"决策 · {decision_card.get('code', '')}",
                            model=req.llm.model,
                        )
                await decisions.create_decision(
                    thread_id=effective_tid,
                    code=decision_card.get("code", ""),
                    name=decision_card.get("name"),
                    target_price=decision_card.get("target_price") or 0.0,
                    entry_low=decision_card.get("entry_low") or 0.0,
                    entry_high=decision_card.get("entry_high") or 0.0,
                    stop_loss=decision_card.get("stop_loss") or 0.0,
                    take_profit=decision_card.get("take_profit") or 0.0,
                    cadence=decision_card.get("cadence") or [],
                    basis_type=decision_card.get("basis_type") or "model",
                    model_versions_json=decision_card.get("model_versions_json") or {},
                    assumptions=decision_card.get("assumptions") or [],
                    citations=decision_card.get("citations") or [],
                    raw_artifact=decision_card,
                )
            except Exception as persist_err:
                # 入库失败不阻塞流（用 console.error 记录）
                print(f"[runner] decision persist failed: {persist_err}", flush=True)
```

注意 3 个关键点：
1. **评审 #2 修法**：`effective_tid` 为空时**后端自动建 thread**，前端不传也不崩
2. **兜底**：前端传了 thread_id 但因网络等原因后端还没建好时，runner 检查并建（防 FK 违反）
3. **入库失败不阻塞流**：decisions 表写失败只 `console.error`，主流程继续

- [ ] **Step 7.4：跑测试验证通过**

```bash
.venv/bin/python -m pytest tests/test_runner.py::test_run_agent_persists_decision_card tests/test_runner.py::test_run_agent_creates_thread_when_none_provided -v
```

期望：两个测试都 PASS。

- [ ] **Step 7.5：跑全量回归**

```bash
.venv/bin/python -m pytest -m "not live" --ignore=tests/test_api.py -v 2>&1 | tail -5
```

期望：71 passed（68 + Task 7 新增 2 + Task 4 decision_failed 1 已算在 68 内）。

- [ ] **Step 7.6：commit**

```bash
cd /vol2/1000/code/stockclaw
git add backend/runner.py frontend/src/hooks/useAgentStream.ts backend/tests/test_runner.py
git commit -m "feat(runner): decision_artifact 入库 decisions 表

- emit decision_artifact + citations 后调 decisions.create_decision
- thread_id / code / target_price / stop_loss / cadence / basis_type /
  model_versions_json / assumptions / citations / raw_artifact 全字段入库
- status='active' 默认值；Phase 3 scheduler 会扫这个字段做收益追踪
- 入库失败不阻塞流，只 console.error
- useAgentStream: fetch 时用 realThreadId 让外键能对上"
```

---

## Phase 1.5 出口验证

完成 Task 1-7 后跑一遍：

```bash
# 1. 全量后端测试
cd backend && .venv/bin/python -m pytest -m "not live" --ignore=tests/test_api.py -v 2>&1 | tail -5
# 期望：70+ passed

# 2. 前端构建
cd ../frontend && npm run build 2>&1 | tail -3
# 期望：built successfully

# 3. 真机端到端
cd backend && .venv/bin/python -m uvicorn app:app --port 8900 --reload &
cd ../frontend && npm run dev &
```

浏览器 `/agent` 走一遍：
- ✅ 刷新页面 sidebar 显示历史会话
- ✅ 首次发消息自动创建会话（标题取前 30 字）
- ✅ decision 意图 5 个 `tool_trace` 折叠小药丸可见
- ✅ 首字延迟从 5s 降到 ~0s（并发）
- ✅ 工具全失败时不推全 0 决策卡（改报 error）
- ✅ 收藏决策卡 + 刷新 → decisions 表有记录
- ✅ 删除会话 → conversations 自动 CASCADE 清空
- ✅ A 股 basis_type 为 `model`（mootdx 装齐）

---

## Self-Review

### Spec 覆盖

| Phase 1.5 改进项 | Task |
|---|---|
| 装齐 mootdx/akshare | Task 1 |
| current_price=0 不推半成品 | Task 2 |
| tool_trace 事件 emit | Task 3 |
| 5s 首字延迟消除 | Task 4 |
| threads CRUD 端点 | Task 5 |
| 前端会话列表全链路 | Task 6 |
| decision_artifact 入库 | Task 7 |

7 项全覆盖。

### 已知遗留（Phase 2）

- panel_valuation 等 5 个 panel agents（多 agent 讨论）
- planner + replanner（plan-execute）
- max_iterations 硬上限
- decisions 表的收益追踪 scheduler（`status` 字段更新）
- ChartRenderer / TableRenderer
- LLM tool selection（让 LLM 决定调哪些 quant 工具）

### 类型一致性检查

- `AgentState.tool_traces`: `list[dict]` — Task 3 加，Task 4 用
- `_invoke` 返回 `tuple[dict, dict]` — Task 3 改，Task 3 测试同步改
- `decisions.create_decision` 签名：Task 7 调用与 `persistence/decisions.py:11` 完全对齐
- `agentApi.saveMessage` 路径 `/agent/threads/:id/messages` — Task 6 前端 + Task 5/6 后端一致

### Placeholder 扫描

无 TBD / TODO / "implement later" / "appropriate error handling" 等占位符。所有代码块完整。

---

## 附录：评审响应（2026-07-10）

针对 `docs/superpowers/review/2026-07-09-phase-1.5-agent-completion-review.md` 的 6 项评审意见全部采纳：

| # | 评审项 | 处置 | 落地位置 |
|---|---|---|---|
| 1 | Critical: Task 2 guard 触发时 Task 4 漏发 `error` 事件 | Task 4 run_agent 加 `if graph_intent == "decision_failed": yield {"type": "error", ...}; return` + 新增 `test_run_agent_emits_error_when_decision_fails` 测试 | Task 4 Step 4.3 + 4.4b |
| 2 | Critical: Task 7 FK 违反（thread_id 为空时 decision_id 不在 threads 表） | runner 持久化前检查 effective_tid：为空就后端自动建 thread；前端已传但表里没时也兜底建；新增 `test_run_agent_creates_thread_when_none_provided` | Task 7 Step 7.1 + 7.3 |
| 3 | Important: Task 4 并发 UX——traces 非实时 | 接受妥协（真正实时需 graph 改 `astream_events` + `asyncio.Queue`，Phase 2 范畴）。Goal 段和 Task 4 目标段明确标注 | Goal 段 + Task 4 目标段「两个明确 UX 妥协」 |
| 4 | Important: Task 6 user 消息在 finally 里归档，断网/关 tab 丢 | user 消息改为 SSE fetch **之前** POST；finally 块只归档 assistant | Task 6 Step 6.4 |
| 5 | Minor: `tempfile.mktemp` 已弃用 | Task 5/7 测试 fixture 改用 pytest `tmp_path` | Task 5 Step 5.1 + Task 7 Step 7.1 |
| 6 | Minor: 让前端用 `crypto.randomUUID()` 提供 thread id | Task 5 Pydantic 加 `id: str \| None = None` 字段；Task 6 useAgentStream 直接 `crypto.randomUUID()` 生成 tid POST 给后端，删除原 `local-xxx → real-xxx` 映射逻辑；Task 5 加 `test_create_thread_accepts_client_id` | Task 5 Step 5.1 + 5.3 + Task 6 Step 6.4 |

零拒绝。6 项评审全部落地，plan 已达可执行状态。

---

**Plan 完成，存到 `docs/superpowers/plans/2026-07-09-phase-1.5-agent-completion.md`。**

接下来用 Subagent-Driven 还是 Inline 执行？
